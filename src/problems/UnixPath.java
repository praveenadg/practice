package problems;

class UnixPath {
//    public String simplifyPath(String path) {
//        StringBuilder newPath=new StringBuilder();
//        for(int i=0;i<path.length(); i++){
//
//            if(path.charAt(i)=='/'){
//                newPath=newPath.append("/");
//                if((i>0 && path.toString().charAt(i-1)=='/')){
//                    newPath=newPath.toString().substring(0,newPath.length()-1);
//                }
//            }
//            if(i>0 && path.charAt(i)=='.'){
//                newPath=newPath.toString().substring(0,newPath.length()-newPath.lastIndex("/"));
//                if(path.charAt(i-1)=='.'){
//                    newPath=newPath.toString().substring(0,newPath.length()-newPath.toString().lastIndexOf("/"));
//                    // newPath=newPath.subString(0,newPath.length()-newPath.lastIndex("/"));
//                }
//            }
//            if(i==path.length()-1 && path.charAt(i)=='.'){
//            }
//        }
//        return newPath.toString();
//    }

}